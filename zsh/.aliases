#!/usr/bin/env bash

# Common
alias c="clear"
alias hg="history|grep"

# Replacements
alias vim="nvim"
alias cat="bat"

# Network/ip
alias ip="ipconfig getifaddr en0"

# Docker
alias dops="docker ps -a"
alias dols="docker image ls"
